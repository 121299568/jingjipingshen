# 经济评审系统 workflow 走通性与权限配置审计报告

> 审计时间：2026-09-07
> 审计对象：backend/server.js、frontend/index.html、backend/src/db.js、backend/src/memoryStore.js
> 测试环境：本地 DB_DRIVER=memory 临时实例（已销毁）

## 一、流程图概述

用户提供泳道图把经济评审分为三个阶段、四个参与方：

| 阶段 | 各事业部 | 研发中心 | 评审专家 | 外部会计师事务所团队 |
|------|----------|----------|----------|----------------------|
| 资料预审阶段 | 提交评审申请、提交材料 | 材料预审、确定并下发评审清单 | — | — |
| 现场评审阶段 | — | 组织评审会、计算平均值、判断是否确认、结果校核 | 现场对工作量打分 | — |
| 成果确认阶段 | 是否确认 | 汇总结果并发起确认、复核后导入结果 | — | 结果校核 |

核心路径：事业部申请 → 事业部传资料 → 研发中心预审并下发清单 → 研发中心组织评审会 → 专家打分 → 研发中心算均值/确认/校核 → 研发中心汇总并发起事业部确认 → 事业部确认 → 研发中心复核导入 → 结束。

---

## 二、逐项走通性检查

### 阶段 1：资料预审阶段

| 流程图节点 | 系统现有功能 | 是否可跑通 | 问题/风险 |
|------------|--------------|------------|-----------|
| 提交经济评审申请 | 无独立的「申请」单据，事业部直接 `POST /api/projects` 创建项目 | ⚠️ 可跑通，但概念缺失 | 流程图有「申请→材料」两步，系统里只有「创建项目」一步，缺少预审前的申请态 |
| 提交相关材料 | `POST /api/projects/:id/files` 上传资料；上传估算表 xlsx 会自动调用 `parseProjectExcel` 抽取成本明细 | ✅ 可跑通 | 已验证事业部可上传；估算表解析已生效 |
| 研发中心预审材料 | 无预审动作/状态标记，rd 只能查看/下载 | ❌ 未实现 | 缺少「预审通过/不通过」动作；无法退回补材料 |
| 确定并下发经济评审清单 | `PATCH /api/sessions/:id` 可把批次状态 `pending→in_progress`；可把项目挂到批次 | ⚠️ 部分可跑通 | 「下发清单」没有独立数据载体，只是改批次状态 |

**本阶段结论：可完成资料上传，但缺少申请单据、预审动作、退回机制。**

---

### 阶段 2：现场评审阶段

| 流程图节点 | 系统现有功能 | 是否可跑通 | 问题/风险 |
|------------|--------------|------------|-----------|
| 组织经济评审会 | 改批次状态为 `in_progress` | ⚠️ 仅有状态标记 | 无会议时间、地点、参会专家、议程等数据 |
| 专家现场对工作量打分 | `POST /api/estimates` 按 `work_item_id` 提交人天 | ❌ 逻辑有严重缺陷 | 见下「致命缺陷 1、2」 |
| 计算工作量平均值 | `/api/projects/:id/cost` 会按 `expertEstimates` 计算 `avg_days` | ✅ 可跑通 | 计算逻辑存在，依赖专家正确提交 |
| 是否确认 → 否 → 重新计算 | `POST /api/confirmations` 可提交确认/驳回 | ⚠️ 可提交，但角色错位 | 确认/驳回只能 expert/accountant 提交，事业部无法参与（流程图由事业部确认） |
| 结果校核 | 外部会计师事务所在 `/api/confirmations` 可标记确认 | ⚠️ 可标记，但缺少复核意见字段 | 无专门的「校核结论」字段 |

**本阶段结论：专家打分入口存在，但「谁能打分、怎么被允许打分」与流程图不匹配，且确认权限主体错误。**

---

### 阶段 3：成果确认阶段

| 流程图节点 | 系统现有功能 | 是否可跑通 | 问题/风险 |
|------------|--------------|------------|-----------|
| 汇总工作量结果并发起结果确认 | 无「发起确认」动作；rd 只能看 cost/确认记录 | ❌ 未实现 | 缺少 rd 发起确认、锁定评估、推送事业部确认的接口 |
| 事业部是否确认 | 事业部无权限调用 `/api/confirmations` | ❌ 未实现 | 当前确认接口仅允许 `['expert','accountant']` |
| 复核后完成经济评审结果导入 | 无「结果导入」动作；`PATCH /api/projects/:id` 不接受 `status` 字段 | ❌ 未实现 | 项目状态无法推进到 `completed`；无导入/归档动作 |
| 结束 | 无明确结束动作 | ❌ 未实现 | 项目归档、批次归档靠手动改 `session.status` |

**本阶段结论：成果确认阶段整体未对齐流程图，事业部无法确认，rd 无法发起和导入结果。**

---

## 三、当前权限配置合理性检查

### 3.1 各角色权限矩阵（实际代码）

| 功能 | admin | rd | biz | expert | accountant |
|------|-------|----|-----|--------|------------|
| 登录 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 用户/分组/权限管理 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 创建批次 `/api/sessions` | ✅ | ✅ | ❌ | ❌ | ❌ |
| 修改批次状态 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 创建项目 `/api/projects` | ✅ | ✅ | ✅（限本事业部） | ❌ | ❌ |
| 修改项目 | ✅ | ✅ | ✅（限本事业部） | ❌ | ❌ |
| 删除项目 | ✅ | ✅ | ✅（限本事业部） | ❌ | ❌ |
| 查看项目列表 | 全部 | 全部 | 本事业部 | 仅已有评估记录的项目 | 仅已有评估记录的项目 |
| 查看项目详情 | 全部 | 全部 | 本事业部 | 仅已有评估记录的项目 | 仅已有评估记录的项目 |
| 上传文件 | 全部 | 全部 | 本事业部 | ❌ | ❌ |
| 下载/查看文件 | 全部 | 全部 | 本事业部 | 已分配才可 | 已分配才可 |
| 提交专家评估 `/api/estimates` | ❌ | ❌ | ❌ | ✅（任意项目） | ✅（任意项目） |
| 提交确认 `/api/confirmations` | ❌ | ❌ | ❌ | ✅ | ✅ |
| 统计分析 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 批次全量下载 | ✅ | ❌ | ❌ | ❌ | ❌ |

### 3.2 发现的权限/流程缺陷

#### 致命缺陷 1：专家「被分配」机制是鸡生蛋问题
- 代码判定专家能否看到项目的唯一依据：`db.store.expertEstimates.some(e => e.project_id === p.id && e.expert_id === user.id)`。
- 这意味着**专家必须先提交过评估，才能看到项目**。
- 实际上 rd/管理员根本没有办法把专家「分配」到项目上，专家登录后项目列表为空，无法进入打分页面。
- 实测：expert01 登录后项目数为 0；直接访问未分配项目返回 `403 无权查看`；一旦用接口伪造评估记录，立刻能看到项目。

#### 致命缺陷 2：专家打分接口未校验项目/工作项归属
- `POST /api/estimates` 仅校验 `role === 'expert' || role === 'accountant'`，未校验：
  - 该专家是否被指派到该项目；
  - `project_id` 是否存在；
  - `work_item_id` 是否属于该项目。
- 实测：向 `project_id=9, work_item_id=9999` 提交评估，返回 `200` 成功，产生一条无效记录。
- 这会导致任意专家可以给任意项目、任意不存在的工作项打分，数据隔离被绕过。

#### 缺陷 3：事业部无法参与成果确认
- 流程图明确要求「各事业部」在成果确认阶段进行「是否确认」。
- 系统里 `/api/confirmations` 仅允许 `expert/accountant`，事业部 `403`。
- 同时研发中心也没有「发起确认」的动作，确认记录是专家/会计师自己任意提交的。

#### 缺陷 4：项目状态无法按流程推进
- `PROJECT_FIELDS` 不包含 `status`，所以 `PATCH /api/projects/:id` 传入 `status: completed` 会被忽略，项目永远停留在 `draft`（创建时强制）或初始值。
- 流程图里的「复核后完成经济评审结果导入」没有对应接口。

#### 缺陷 5：缺少退回/预审不通过机制
- 流程图里有「材料预审」失败路径和「是否确认→否→返回现场评审」路径。
- 系统目前没有任何「退回」「驳回并附理由」「重新上传」的状态流转。

#### 缺陷 6：外部会计师事务所团队角色边界模糊
- 泳道图把「外部会计师事务所团队」单列，但系统只有 `accountant` 一个角色，且该角色与评审专家共用同一套评估/确认接口。
- 「结果校核」应该由会计师事务所做，但当前会计师也可以像专家一样提交 `estimates`，职责混同。

#### 缺陷 7：批次状态与项目状态未联动
- 批次 `status` 只是 `pending/in_progress/completed` 标记，没有校验：
  - 启动评审会前是否已上传估算表；
  - 归档前是否已完成确认；
  - 归档后是否禁止修改评估。
- 当前任何人有权限就可以改，没有流程锁。

---

## 四、结论：是否能按流程图走通？

| 流程阶段 | 走通评级 | 说明 |
|----------|----------|------|
| 资料预审阶段 | ✅ 通 | 管理员/研发中心上传或新增项目 → 清单齐全度核对 → 研发中心预审（通过/退回） |
| 现场评审阶段 | ✅ 通 | 下发清单与会议信息 → 分配专家/会计师 → 打分（校验分配与工作项归属）→ 算均值 → 结果校核 |
| 成果确认阶段 | ✅ 通 | 研发中心发起确认 → 事业部确认/退回 → 研发中心复核归档 → 批次自动归档 → 归档后锁定 |

**总体结论（整改后）：系统已可按该 workflow 图完整走通。**
全部阻塞点（专家分配、权限边界、状态流转、事业部确认）与遗留项（下发清单、评审会数据、
结果校核、批次流程锁、归档锁定）均已完成整改并通过本地 + 生产双环境验证。

---

## 五、修复建议（按优先级）

### P0：阻塞流程跑通

1. **新增专家/会计师分配机制**
   - 新增数据表/集合 `projectExperts`（或扩展 `expertEstimates` 增加 `assigned` 标记）。
   - 新增接口 `POST /api/projects/:id/assign-experts`（rd/admin 可用），允许把专家/会计师挂到项目上。
   - `filterByDept` 对 expert/accountant 的判定改为：已分配 或 已有评估记录。

2. **修复专家打分权限**
   - `POST /api/estimates` 必须校验当前用户已被分配到该项目，且 `work_item_id` 属于该项目。
   - 未分配的专家提交评估应返回 `403`。

3. **放开事业部确认权限，限制专家/会计师角色**
   - `/api/confirmations` 应允许 `biz`（本项目事业部）、`rd`、`admin` 提交确认/驳回。
   - 专家/会计师应只能提交评估，不能提交确认；或把确认动作拆分为「专家认可」和「事业部/会计师事务所确认」两层。

4. **让项目状态可推进**
   - `PATCH /api/projects/:id` 允许 `status` 字段。
   - 增加状态机：`draft → reviewing → pending_confirm → completed`。
   - 只有 rd/admin 能把项目推进到 `completed`。

### P1：补齐流程图细节

5. **增加材料预审动作**
   - 新增 `POST /api/projects/:id/pre-review`（rd 用），状态 `draft → reviewing` 或 `draft → rejected`。
   -  rejected 时要求填写原因，事业部可补材料后重新提交。

6. **增加 rd 发起确认动作**
   - 新增 `POST /api/projects/:id/initiate-confirmation`（rd 用），锁定当前评估均值，生成待确认记录给事业部确认。

7. **结果导入/归档**
   - 事业部全部确认后，rd 可调 `POST /api/projects/:id/finalize` 把项目置为 `completed`，并同步归档批次。

### P2：体验与合规

8. **明确会计师事务所团队职责**
   - 建议把 `accountant` 的权限从「可打分」改为「仅可校核/确认」，避免与专家角色混用。

9. **批次流程校验**
   - `in_progress` 前校验估算表已上传；
   - `completed` 前校验本项目已完成确认或已导入结果。

10. **操作日志与审计**
    - 所有状态推进、确认/驳回、分配/取消分配都写入 `workflowLogs`。

---

## 六、测试脚本摘要

本次审计使用本地内存模式临时启动服务，关键验证结果：

- 事业部 `biz_gdw` 可创建项目 ✅
- 未分配专家访问项目 → `403 无权查看该项目` ✅（验证了隔离）
- 专家向任意 `work_item_id=9999` 提交评估 → `200 成功` ❌（验证了打分接口未校验）
- 提交评估后专家可看到项目 ✅（验证了「先打分再可见」的倒置逻辑）
- 事业部提交确认 → `403 无权限操作` ❌（验证了事业部无法参与确认）
- rd 通过 `PATCH` 改项目状态为 `completed` → 被忽略，状态仍为 `draft` ❌

---

## 七、P0/P1 修复实施结果（commit `6287112`，已部署）

> **用户纠正**：会计师事务所也拥有专家进行工作量评估的角色 → `accountant` 与 `expert` **同具评估权**，
> 不按原 P2 建议把会计师降级为「仅校核」。

### 7.1 完成项

| 编号 | 建议 | 落实情况 |
|------|------|----------|
| P0-1 | 新增专家/会计师分配机制 | ✅ 新增 `projectAssignments` 集合（json/mysql/memory 三驱动适配）+ `POST/GET /api/projects/:id/assign(ments)`（rd/admin，替换式）；`filterByDept`、项目详情、文件访问全部改为「已分配 或 已有评估」 |
| P0-2 | 修复专家打分归属校验 | ✅ `POST /api/estimates` 校验：①已分配到该项目 ②`work_item_id` 必须属于该项目 |
| P0-3 | 放开事业部确认权限 | ✅ 新增项目级 `POST /api/projects/:id/confirm`（`biz` 限本事业部，rd/admin 可代确认）+ `biz_confirmed` 字段 |
| P0-4 | 项目状态可推进 + 状态机 | ✅ `PROJECT_STATUS_FLOW`：`draft→reviewing→pending_confirm→completed`，`rejected` 为退回分支；`PATCH /projects/:id` 开放 `status`（仅 rd/admin） |
| P1-5 | 材料预审动作 | ✅ `POST /api/projects/:id/pre-review`（通过→`reviewing`；退回→`rejected`，退回必填原因） |
| P1-6 | rd 发起确认 | ✅ `POST /api/projects/:id/initiate-confirmation`（须已有评估数据） |
| P1-7 | 结果导入/归档 | ✅ `POST /api/projects/:id/finalize`（须事业部已确认；该批次项目全完成则自动归档批次） |
| 附加 | 会计师评估角色 | ✅ `/estimates` 保持 `auth(['expert','accountant'])`，会计师可被分配并参与评估，评估记录打 `expert_role` 标记 |
| 附加 | 审计日志 | ✅ 分配/预审/发起确认/确认/退回/归档全部写入 `workflowLogs` |

### 7.2 前端改动

- 项目行新增「流程」按钮（全员可见）与「分配」按钮（rd/admin）。
- 新增「分配评审人员」弹窗：按角色列出专家与会计师事务所，多选保存（覆盖式）。
- 新增「项目评审流程」弹窗：展示当前状态、已分配人员、退回原因，并**按角色与当前状态**给出可用操作
  （预审通过/退回、发起成果确认、复核归档、事业部确认/退回）。
- 状态徽章覆盖新状态；工作量评估页的专家输入按「是否分配」控制；项目下拉不再只列进行中批次。

### 7.3 验证结果

- **本地（memory 驱动）**：18 项断言全 PASS —— 含会计师可评估、未分配提交 403、工作项不归属 400、
  重复评估 400、退回必填原因、未确认不可归档、事业部越权 403、非法流转拒绝。
- **生产（阿里云 `47.94.59.129:3000`，MySQL 驱动）**：9 项断言全 PASS —— 新表自动创建、
  分配与可见性、预审推进、无评估时发起确认被拒；测试项目已清理。

### 7.4 后续已补齐项

原 7.4 列出的 P2 与阶段遗留问题，已在**第二节「全面整改」**中全部处理，详见下文第八节。

---

## 八、剩余问题全面整改（commit `6287112` 之后，已部署）

针对第一/二/三节中标记为 ⚠️ / ❌ 的全部剩余项：

| 原问题 | 整改措施 |
|--------|----------|
| 缺少「提交经济评审申请」单据 | ✅ **经用户确认无需此环节**：评审项目清单由管理员/研发中心通过 Excel 导入或新建项目直接录入，草稿态即可由研发中心直接预审。`submitted` 状态与 `POST /api/projects/:id/submit` 路由已移除，状态机恢复 `draft → reviewing` |
| 「下发经济评审清单」无独立数据载体 | ✅ 批次新增 `checklist`（材料类别清单）字段，`PATCH /api/sessions/:id` 可配置即下发；项目详情返回 `checklist_status` 逐项齐全度；未配置时回退 8 类默认清单 |
| 组织评审会无会议数据 | ✅ 批次新增 `meeting_location`、`meeting_agenda`（会议时间复用 `review_time`）；前端「批次设置」弹窗可维护并在批次行展示 |
| 结果校核无专门结论字段 | ✅ 新增 `POST /api/projects/:id/verify`（expert/accountant，须已分配），结论 `approved/adjust/rejected`，非认可必填说明，写入 `audit_conclusion`（含结论人/角色/时间/说明） |
| 批次启动前未校验资料 | ✅ `PATCH /sessions/:id` 置 `in_progress` 前校验：批次内所有项目必须已上传成本估算表，否则 400 并列出缺失项目 |
| 批次归档前未校验项目状态 | ✅ 置 `completed` 前校验：批次内所有项目必须已 `completed`，否则 400 并列出未完成项目 |
| 归档后仍可改评估/传资料 | ✅ 新增 `projectLocked()`：项目已归档或所属批次已归档后，`POST /api/estimates` 与 `POST /api/projects/:id/files` 一律 403 |
| 会计师事务所职责 | ⚠️ 按用户纠正：**会计师事务所与专家同具工作量评估角色**，故保留其评估权，同时新增校核（`verify`）职责，不再降级为「仅校核」 |

### 状态机（最终）

```
draft ──研发中心预审通过──▶ reviewing ──发起确认──▶ pending_confirm ──复核归档──▶ completed
  ▲                         │                        │
  └─────── 退回 ─────────────┴──────────────────────┴── rejected ─────────────┘
                    （退回后可补充材料，重新由研发中心预审）
```

### 验证

- **本地（memory 驱动）**：新增 18 项断言全 PASS（跨事业部拒绝、清单下发与齐全度、批次启动/归档双锁、校核权
  限与必填说明、归档后禁改评估、含工作项的完整主流程）。
- **生产（阿里云，MySQL 驱动）**：8 项断言全 PASS，临时项目已清理，未改动任何真实业务数据。

### 仍未纳入（需产品确认，非技术阻塞）

- 评审会参会人签到、会议纪要等更细的会务数据（当前参会人由 `projectAssignments` 体现）。
- 申请单的附件级版本管理（当前以「最后上传时间」与文件序号体现）。
- GitHub 推送仍受沙箱网络限制，本地 commit 待网络恢复后补推。
