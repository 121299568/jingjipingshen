# 内网离线部署方案（索引）

本方案已统一维护在仓库根目录的 **[INTRANET_DEPLOY.md](../../INTRANET_DEPLOY.md)**（独立文档，含阻塞点分析、环境准备、一键部署脚本用法、网络隔离、数据迁移、安全运维、MySQL 进阶、回滚与故障排查）。

- 一键脚本：`setup-intranet.sh`（本目录，`install` / `start` / `stop` / `restart` / `status`）
- 配置模板：`.env.intranet`（本目录）
- systemd 单元模板：`economic-review.service`（本目录）

> 以根目录 `INTRANET_DEPLOY.md` 为准，本文档仅作索引，避免内容漂移。
