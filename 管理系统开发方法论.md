# 管理系统开发方法论（基于「经济评审管理平台」的实践提炼）

> **脱敏声明**：本文档提炼自某企业内部「经济评审管理平台」的开发实践，已对域名、IP、代码仓库、账号口令、密钥、内部路径等敏感信息做脱敏处理，相关处一律以 `<占位符>` 表示，仅保留可复用的工程方法论，可供同类内部管理系统参考。

---

## 0. 适用场景

- 企业内部流程类管理系统（评审、审批、采购、合同、验收等）。
- 团队规模小、需求滚动、需要「边用边改」的快节奏项目。
- 不追求高并发，但要求：**数据不丢、流程可追溯、权限清晰、部署简单**。

---

## 1. 核心设计原则

1. **低运维优先**：能用单文件前端+无构建步骤就别上重型框架；能用进程管理器就别上复杂编排。运维成本往往大于开发成本。
2. **开发/生产数据层分离**：用可替换的数据层适配器（本地文件 / 内存 / 生产数据库）隔离，本地跑通再上生产，互不污染。
3. **同源部署**：前端与后端同一域名/端口，从架构上规避 CORS 与跨域鉴权坑。
4. **状态机驱动流程**：所有业务流转走**唯一状态机入口**，禁止散落各处的 ad-hoc 改状态，否则流程与权限会失控。
5. **双轨闭环**：若企业政策不允许流程全线上化，系统必须同时支持「线下产出 → 表格导入」，形成线上线下两条腿走路的闭环。

---

## 2. 架构方法论

### 2.1 前端
- 单 HTML 文件内联 CSS/JS，组件库走 CDN。改完**必须抽出内联脚本跑语法检查**——单文件一处括号缺失会让整段 `<script>` 解析失败，连登录监听都不绑定，表现为全站 JS 失效（这是本类项目最高频的「点了没反应」根因）。
- 任何交互/动态渲染都要有静态兜底或清晰的空态，避免「假动画/占位逻辑」。

### 2.2 后端
- 轻量 Web 框架 + JWT 鉴权；文件上传用成熟中间件（multer 等）。
- 数据层抽象为统一 `db` 接口，按环境变量（如 `DB_DRIVER`）切换：开发期用本地文件或内存，生产期用关系库。
- 生产关系库对**未知字段用 `extra` JSON 兜底**，新增展示字段无需改表结构，降低上线摩擦。

### 2.3 部署拓扑
- 云主机 + 进程管理器（pm2 类） + 反向代理（HTTPS）。
- 反向代理同时收敛公网暴露面、统一域名、做 TLS。

---

## 3. 功能迭代方法（滚动增量）

1. **先建流程状态机与权限矩阵**：明确「谁、在什么状态、能做什么」，再填功能。这是后续一切权限与数据隔离的基石。
2. **需求常以截图/样例文件驱动**：用户给一张参考表或导出样例，按图实现并对齐格式，比纯文字需求更准。
3. **一次性给多套改造方案时，倾向闭环而非保守**：能一次做透就做透，避免半成品反复来回。
4. **每个功能交付前自问三问**：
   - 真实落地的验证做了吗？
   - 可配置项真的生效吗？
   - 有没有假动画/占位逻辑？
5. **小步快跑、即时提交**：每完成一项改动立即提交并推送，不攒批，保证可回溯。

---

## 4. 文件解析与内容识别（亮点实践）

面向「资料归档/批量挂接」场景，文件命名千奇百怪，不能靠文件名匹配，要**读内容**判断归属。

### 4.1 按扩展名自动分派解析器
| 扩展名 | 解析方式 |
|--------|----------|
| `.xlsx/.xls` | 抽取全部单元格文本 |
| `.pdf` | PDF 文本提取库 |
| `.docx` | 文档文本提取库（仅 OOXML 新格式） |
| 其他（图片、旧版 `.doc` 等） | 返回空文本，走兜底，不报错中断 |

> 解析库需随部署安装进生产依赖目录（`npm install`），并在启动时探测可用性；缺库时优雅降级而非崩溃。

### 4.2 归属判定优先级
1. **文件内容识别**（权重递减）：项目编号（精确）> 项目全称（精确）> 名称连续子串（部分匹配，取最长命中）。
2. **文件名兜底**：仅在内容无法识别时，按文件自身名称模糊匹配。
3. **同文件夹归并**：第一遍逐文件判定后，第二遍按目录归并——只要某文件夹内有 1 个文件能识别项目，**该文件夹下其余未匹配文件（含无法解析的 PDF/图片）一并归属同一项目**。

### 4.3 解析健壮性
- 每个解析分支独立 `try/catch`，失败返回空字符串，不影响整体流程。
- 异步解析（PDF/Word 需读文件内容）要在调用处 `await`，避免把异步结果当成同步判空。

---

## 5. 部署运维方法论

- **自动化脚本**：一键部署脚本负责装运行时、拉仓库、建库建用户、生成配置、进程托管与开机自启。
- **最小变更上线**：只 scp 改动文件 + 重启进程，不必全量重建（前提是依赖已就位）。
- **代码托管与同步**：私有仓库，提交即同步；本地与托管平台之间若被网络代理拦截，采用「代理绕行 + 重试」策略直至推送成功。
- **冷启动容错**：免费/轻量实例首次请求慢是正常现象，health 探测与接口调用都应带合理超时与重试。

---

## 6. 质量保障与验证体系

- **前端改动**：抽出内联脚本做语法体检（`node --check`）。
- **后端改动**：语法检查 + 解析器分支单测（全称/编号/部分名/不匹配/归并 各例）。
- **生产冒烟**（上线后必做）：
  1. `health` 返回 200；
  2. 受限接口**无鉴权返回 401**（确认鉴权未失效）；
  3. 关键导出结构与参考模板**逐列对齐**。
- **真实 e2e**：建临时项目 → 上传真实文件 → 核验匹配/导出 → **删除临时数据并核验无残留**，避免污染生产。

---

## 7. 信息安全与脱敏规范（重点）

1. **密钥绝不进代码仓库**：JWT 等密钥一旦硬编码进仓库（尤其是公开仓库），任何有读权限者即可伪造令牌、越权操作。**应使用环境变量或密钥管理服务**，并立即轮换已泄露的密钥。
2. **初始弱口令必须改**：默认管理员账号/弱口令上线是重大风险，首次部署应强制改密并启用强口令策略。
3. **数据隔离要真隔离**：避免同一含义多字段并存（如两个「事业部」字段混用导致过滤恒空），上线前**补越权用例**验证隔离确实生效。
4. **公网暴露收敛**：安全组只放行必要端口；反向代理做 HTTPS 与访问收敛，避免裸端口直连。
5. **敏感信息不出文档**：对外交付文档一律脱敏——域名、IP、账号、口令、密钥、内部路径、仓库名替换为占位符。
6. **临时测试数据清理**：e2e 产生的临时项目/文件必须删除并核验，防止混入生产统计。

---

## 8. 常见坑 Checklist

- [ ] 单文件前端改完跑语法检查了吗？（一处括号缺失=全站失效）
- [ ] 新增解析库在生产依赖里真正 `install` 了吗？
- [ ] 解析器异步调用处都 `await` 了吗？
- [ ] 部署后 health / 鉴权 401 / 关键导出结构 真测了吗？
- [ ] 临时测试数据清理并核验无残留了吗？
- [ ] 密钥/口令/敏感路径脱敏了吗？
- [ ] 数据隔离字段是否一致、有无越权风险？

---

## 9. 一页纸速查

| 维度 | 关键动作 |
|------|----------|
| 设计 | 低运维、数据层分离、同源部署、状态机驱动、双轨闭环 |
| 架构 | 单文件前端（改完体检）、轻量后端+JWT、extra JSON 兜底、反代 HTTPS |
| 迭代 | 先状态机后功能、截图/样例驱动、小步快跑即时提交 |
| 文件 | 按后缀分派解析器、内容识别>文件名>同文件夹归并、解析失败优雅降级 |
| 部署 | 一键脚本、最小变更上线、代理绕行重试 |
| 验证 | 前端体检、解析单测、生产冒烟、真实 e2e 并清残留 |
| 安全 | 密钥不入仓、弱口令必改、真隔离、暴露收敛、文档脱敏 |

---

*本文档为方法论提炼，不含任何真实环境敏感信息。落地时请结合本组织的安全基线（等保/内网规范）补充具体要求。*
